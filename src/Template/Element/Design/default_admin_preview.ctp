<?php
/**
 * Empty by default, can be changed by creating an admin_preview.ctp in the Widget's Template folder.
 * 
 * Available view variables by default are:
 * - CmsBlock $block
 * - AbstractWidget $widget
 * 
 * Additional view vars can be set by overriding the adminPreview() method in the Widget class.
 */
